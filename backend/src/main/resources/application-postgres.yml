spring:
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/diary_discovery}
    username: ${POSTGRES_USER:diary}
    password: ${POSTGRES_PASSWORD:diary}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
