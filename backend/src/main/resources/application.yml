server:
  port: 8080
  servlet:
    context-path: /

spring:
  profiles:
    default: postgres

sentimentscribe:
  spotify:
    client-id: ${SPOTIFY_CLIENT_ID:3302f8d729f747aea634c70bdc8cc670}
    client-secret: ${SPOTIFY_CLIENT_SECRET:dcfadc850ebc4081a58fc839c2434825}
  tmdb:
    api-key: ${TMDB_API_KEY:e112ddda993eb2247a0602076d5ba0b7}
  auth:
    password: ${SENTIMENTSCRIBE_PASSWORD:123}
  jwt:
    secret: ${SENTIMENTSCRIBE_JWT_SECRET:change-me}
    issuer: ${SENTIMENTSCRIBE_JWT_ISSUER:sentimentscribe}
    ttl-seconds: ${SENTIMENTSCRIBE_JWT_TTL_SECONDS:3600}
  cors:
    allowed-origins:
      - ${SENTIMENTSCRIBE_CORS_ORIGIN:http://localhost:3000}

logging:
  level:
    com.sentimentscribe.data: INFO
